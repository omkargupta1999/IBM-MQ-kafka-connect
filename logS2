Perfect 👍
Below is a **quick, no-nonsense runbook** you can follow **from your side (OpenShift + Kafka)** to prove that *everything on our end is correct* before MQ team fixes their side.

---

# 🔎 **NEFT MQ ↔ Kafka – Verification Runbook (Our Side)**

## 1️⃣ Kafka Cluster Health (Baseline)

```bash
oc get pods -n dev-kafka
```

✅ Expected:

* All **broker** and **controller** pods → `Running`
* **KafkaConnect pods** → `Running`
* **Entity Operator** → `2/2 Running`

👉 If this is green, Kafka infra is OK.

---

## 2️⃣ KafkaConnect API Health

```bash
oc exec -it dev-kafka-connect-mq-test-connect-0 -n dev-kafka -- \
curl http://localhost:8083/
```

✅ Expected:

```json
{"version":"3.9.0","commit":"...","kafka_cluster_id":"..."}
```

---

## 3️⃣ Verify MQ Plugins Loaded

```bash
oc exec -it dev-kafka-connect-mq-test-connect-0 -n dev-kafka -- \
curl http://localhost:8083/connector-plugins
```

✅ Must contain:

* `com.ibm.eventstreams.connect.mqsource.MQSourceConnector`
* `com.ibm.eventstreams.connect.mqsink.MQSinkConnector`

👉 If visible → **image + jars are correct**

---

## 4️⃣ Verify KafkaConnector CRs Exist

```bash
oc get kafkaconnector -n dev-kafka
```

Expected:

* `neft-mq-source`
* `neft-mq-sink`

---

## 5️⃣ Check Connector Runtime Status

```bash
oc get kafkaconnector neft-mq-source -n dev-kafka -o yaml
oc get kafkaconnector neft-mq-sink   -n dev-kafka -o yaml
```

### What matters:

```yaml
connectorStatus:
  connector:
    state: RUNNING
  tasks:
    - state: FAILED
```

❗ **Important**

* `connector = RUNNING` → Kafka Connect is OK
* `task = FAILED` → External dependency issue (MQ)

---

## 6️⃣ Confirm Exact Failure Reason (Proof)

From the same output, confirm this error:

```
MQRC_UNKNOWN_CHANNEL_NAME (2540)
AMQ9520: Channel not defined remotely
```

✅ This error means:

* MQ host reachable
* MQ port reachable
* Queue Manager reachable
  ❌ **Only channel definition is missing / wrong on MQ**

👉 **This is 100% MQ-side issue**

---

## 7️⃣ Verify Kafka Topics Exist

```bash
oc exec -it dev-kafka-cluster-broker-0 -n dev-kafka -- \
bin/kafka-topics.sh --bootstrap-server localhost:9093 --list | grep neft
```

Expected:

* `neft.raw.kafka.topic`
* `neft.processed.kafka.topic`

---

## 8️⃣ Verify Kafka Can Accept Data (Optional Test)

```bash
oc exec -it dev-kafka-cluster-broker-0 -n dev-kafka -- \
bin/kafka-console-producer.sh \
--bootstrap-server localhost:9093 \
--topic neft.raw.kafka.topic
```

Type:

```
test-message
```

If accepted → Kafka side is fully functional.

---

## 9️⃣ Network Proof (Optional but Strong)

```bash
oc exec -it dev-kafka-connect-mq-test-connect-0 -n dev-kafka -- \
nc -zv 10.176.10.213 2001
```

✅ If connection succeeds → Network + firewall OK

---

# ✅ FINAL CONCLUSION (FOR YOU & MANAGEMENT)

✔ Kafka Cluster – OK
✔ Kafka Connect – OK
✔ MQ Plugins – Loaded
✔ Connectors – Created & Running
✔ Network – Reachable
❌ MQ Channels – **NOT defined / NOT accessible**

📌 **There is nothing left to fix on our side.**

---

If you want next:

* 📄 Convert this into **Word / PDF**
* 📧 Attach this as **technical proof to MQ email**
* 📋 Create **production checklist**
* 🧠 KT document for handover

Just say 👍
